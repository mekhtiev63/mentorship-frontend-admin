export type RoadmapBlock = {
  id: string
  title: string
  order: number
  active: boolean
}

export type RoadmapMaterial = {
  id: string
  blockId: string
  title: string
  order: number
  active: boolean
}
